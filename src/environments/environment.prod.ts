// In questo file possiamo passare delle personalizzazioni per l'ambiente di produzione con questi paramtri (è un oggetto)

// Definizione della costante di ambiente "environment"
export const environment = {
  production: true // La proprietà "production" è impostata su "true"
};

// Questo codice definisce una costante di ambiente che viene utilizzata per specificare se l'applicazione Angular è in modalità di produzione o di sviluppo. In questo caso, l'applicazione è in modalità di produzione.
