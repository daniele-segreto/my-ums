export interface UserInterface {
  id: number; // aggiungo l'id (che non sarà modificabile)
  name: string;
  lastname: string;
  email: string;
  fiscalcode: string;
  province: string;
  phone: string;
  age: number;
}
